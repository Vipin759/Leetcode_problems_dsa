int* shuffle(int* nums, int numsSize, int n, int* returnSize){
    *returnSize = numsSize;

    int *ans = malloc(numsSize * sizeof(int));

    for(int i = 0; i < n; i++){
        ans[2i]     = nums[i];
        ans[2i + 1] = nums[i + n];
    }

    return ans;
}
