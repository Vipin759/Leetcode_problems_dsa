char* findDifferentBinaryString(char** nums, int numsSize) {
    int n = strlen(nums[0]);
    char* res = (char*) malloc((n+1) * sizeof(char));
    for(int i = 0; i<n; i++){
        if(nums[i][i] == '0'){
            res[i] = '1';
        }
        else{
            res[i] = '0';
        }
    }
    res[n] = '\0'; 
    return res;
    
}
